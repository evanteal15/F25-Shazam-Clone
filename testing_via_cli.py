
# TODO by Friday Sept 26th (Dennis)

# 1) create dataset of WAV files, loadable via zip file loader function (no dependencies)
# 2) function to create fingerprints of all wav files (default SR, lowSR)
# 3) function to create labeled test set of 5s samples from dataset songs (SR, lowSR)
# 4) function to create labeled test set of same 5s samples with added noise (SR, lowSR)
# 5) manual dataset of labeled microphone samples (play via phone into macbook mic)
# 6) an evaluator to display false negative rate with/without noise augmentation
